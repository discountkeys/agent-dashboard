[build]
  publish = "."
  command = "echo 'No build required for static HTML'"

[dev]
  command = "echo 'No dev server needed'"
  target_port = 3000

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"
